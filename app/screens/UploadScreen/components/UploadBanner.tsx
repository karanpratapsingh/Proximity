import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

const UploadBanner = props => (
  <View style={styles.container}>
    <Text>UploadBanner</Text>
  </View>
);

const styles = StyleSheet.create({
  container: {
    
  }
});

export default UploadBanner;