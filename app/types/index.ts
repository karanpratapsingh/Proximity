export type ThemeColors = {
  accent: string,
  base: string,
  text01: string,
  text02: string,
  search: string,
  white: string
};

export type ThemeType = {
  type: string,
  colors: ThemeColors
};

